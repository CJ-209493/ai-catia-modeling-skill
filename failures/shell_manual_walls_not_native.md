# Failure Memory: Manual Walls Are Not Native Shell

Manual inner/outer wall construction can look like a hollow box, but it is not a CATIA native `Shell` feature.

Classification:

- Use `GEOMETRY_EQUIVALENT` if geometry is visually close.
- Use `NATIVE_SUCCESS` only when a `Shell` feature exists and verification passes.
