# Failure Memory: Explicit Holes Are Not Native Circular Pattern

Creating repeated holes one by one is not native Circular Pattern.

Classify as `GEOMETRY_EQUIVALENT` when hole positions match but no `CircPattern` exists.

Classify as `NATIVE_SUCCESS` only when a native `CircPattern` feature exists and verification passes.
